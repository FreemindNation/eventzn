import React from 'react'
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Profile',
};


function page() {
  return (
    <div>Profile</div>
  )
}

export default page;